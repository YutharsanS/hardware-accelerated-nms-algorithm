"""3D Gaussian Splatting analysis for the bitonic sorting-network project.

Phase 0 of the plan: reimplement the reference rasterizer's preprocess stage so the
per-tile depth-sorting workload can be measured without a GPU, then decide the sorter
architecture from that data.
"""
