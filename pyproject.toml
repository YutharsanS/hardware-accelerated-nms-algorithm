[project]
name = "hardware-accelerated-nms-algorithm"
version = "0.1.0"
description = "Hardware-accelerated NMS algorithm: VHDL RTL + Python golden model + dev tooling"
requires-python = ">=3.12"
dependencies = [
    "numpy>=2.5.2",
]

[dependency-groups]
dev = [
    "nbstripout>=0.9.1",
    "ruff>=0.16.4",
]

[tool.ruff]
force-exclude = true

extend-exclude = [
    ".vscode",
    "/usr/lib/python3.12",
    ".venv"
]

[tool.ruff.lint.per-file-ignores]
"test_*.py" = ["S101", "D", "T201"]
"*_test.py" = ["S101", "D", "T201"]
"**/test_*.py" = ["S101", "D", "T201"]
"**/*_test.py" = ["S101", "D", "T201"]
"tests/**/*.py" = ["S101", "D"]
"notebooks/**/*.ipynb" = ["T201", "D"]
"models/gen_vectors.py" = ["T201"]
"models/bench_cpu.py" = ["T201"]

[tool.ruff.lint]
extend-select = [
    "D",    # Pydocstyle – docstring formatting
    "COM",  # Flake8-commas – trailing/comma issues
    "T201", # Flake8-print - print statement
    "ANN", # Flake8-annotations – enforces type annotation style
]

ignore = ["D100", "COM812"]

[tool.ruff.lint.pydocstyle]
convention = "google"
